# En desarrollo local no hace falta nada acá — Vite ya redirige /api al backend
# (ver vite.config.js). Dejá VITE_API_URL vacío o sin definir.

# En producción (Vercel), poné acá la URL pública del backend ya desplegado:
# VITE_API_URL=https://tu-backend.vercel.app
